# ==============================================================================
# Brewfile - Yosemite Dependency Tracking Index
# ==============================================================================

# Force Homebrew to pull binary source files natively instead of downloading modern broken pre-compiled bottles
cask_args appdir: "/Applications"

# Core Development Requirements
brew "git"            # Upgrades system from legacy Git 1.9 to latest stable
brew "tmux"           # Session management tool matrix
brew "vim"            # Compiled text processing engine
brew "wget"           # Insecure fallback pipeline asset grabber
